emails = document.getElementById("emails")

emails.innerHTML = "<%= j paginate(@emails, remote: true) %>"
emails.innerHTML += "<%= j render(@emails, deletable: false, inbox: true, starred: false, users: @users) %>"
